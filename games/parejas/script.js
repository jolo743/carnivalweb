const cardIcons = ['rocket', 'controller', 'celebration', 'circus', 'popcorn', 'sharp-five', 'sparkle', 'ticket']

let  cards= [...cardIcons, ...cardIcons]
let flippedcards = []
let matchedpairs = 0
let score = 0
